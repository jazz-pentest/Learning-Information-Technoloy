## Jump Server
- its part of networking and systems
- it gives remote access over ssh
- it holds authetication keys
- its its own server; could be as simple as a raspberry pie
- its the gateway to access other server
- its going to be in a segmented zone (typically in a DMZ)
